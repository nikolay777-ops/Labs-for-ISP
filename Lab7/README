К данной лабораторной работе прилагаю 3 файла: 
---MathFunc.cs---
В статическом классе MathFunc определены 3 метода:
1. Swap(ref int a, ref int b) выполняет смену местами значений а и b, данный метод нужен в дальнейшем для поиска НОД.
2. GCD(int a, int b) (GCD - Great Common Divisor) вычисляет наибольший общий делитель с помощью алгоритма Евклида.
3. LCM(int a, int b) (LCM - Lowest Common Multiple) вычисляет наименьшее общее кратное.

---RationalNumber.cs---
Класс RationalNumber реализует 3 системных интерфейса IEqutable<RationalNumber>, IComparable<RationalNumber>, IFormattble.
А также перегрузку операторов -, +, *, /,  >, <, ==, !=, <=, >=; перегрузку операторов явного приведения типов(int, decimal, lomg) и неявного приведения типов(float, double).
При перегрузке операторов сравнения пользовался приведением к общему знаменателю. 
Метод Reduce() выполняет сокращение дроби(если оно возможно).
