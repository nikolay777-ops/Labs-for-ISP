----Что было сделано в 6-й ЛР----

1. Interface.cs 
  Данный файл содержит в себе интерфейсы IEducation с методом Studying() для определения в классе Student данного метода. 
 ICasino содержит в себе методы Casino(byte n), Jackpot(byte type, int cash), CasinoInfo(bool n) для определения данных методов в базовом классе Student.
 
 IDefaultActions содержит методы типичных дел, которые может выполнять любой человек: IsAlive(), CheckDays(), Sleep(), MoneyMake(),
 
 BottlesToMoney() (следовательно для определения в базовом классе Student).
 
 ICompare<T, T1> самописный интерфейс для сравнения 2-х объектов класса по количеству денег, и по уровню образования (SmartCompare(T first, T1 second), 
 MoneyCompare(T first, T1 second)).
 
2. Также были использованы 2 системных интерфейса ICloneable, IComparable. 
  ICloneable необходим если пользователь захочет сменить один вид персонажа на другой (Спорстмен->Бизнесмен или Программист и тд.) без потери заработанных денег, дней, учебных поинтов.
  В ICloneable переопределялся метод Clone() в классе Student.
  
  IComparable (а именно метод CompareTo()) необходим для сортировки List<Student> по одному из параметров (в моём случае по количеству денег). В дальнейшем может пригодится для
  составления таблицы самых богатых игроков.
  
Работа всего сделанного наглядно показана в файле Program.cs
 
